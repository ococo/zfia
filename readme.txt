places directory
================

The code in this folder contains the completed Places
application.

You need to create application/configuration/config.ini from
the supplied default version. The easiest thing to do is
copy the config.ini.default file to config.ini and then
modify if you need to.